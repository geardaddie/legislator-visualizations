# if Bills.find().count() is 0
#   fs = Npm.require('fs')
#
#   fs.readdir './assets/app', Meteor.bindEnvironment((err, res) ->
#     _.each res, (filename) ->
#
#   )
#   # _.each res, (filename) ->
#   #   console.log Assets.getText(filename)
#
#     # _.each(r, function(filename) {
#     #     Assets.getText('myFolder/' + filename);
#     # })
#
      #
      # xml2js.parseString(res, {attrkey: "attr-key"}, function(err,res) {
      #   if (Bills.find().count() === 3) {
      #     Bills.insert(res)
      #   }
      # })
